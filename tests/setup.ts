import { afterAll, afterEach, beforeAll, beforeEach } from 'bun:test';

beforeEach(() => {
  // global setup
});

beforeAll(() => {
  // global setup
});

afterEach(() => {
  // global teardown
});

afterAll(() => {
  // global teardown
});
